# Storer Ponto Extension

## Como rodar

### 1. Mock da API
cd mock-api
npm install
npx ts-node server.ts
# API rodando em http://localhost:3001

### 2. Extensão
cd extension
npm install
npm run build
# Carregar a pasta dist/ no Chrome em chrome://extensions